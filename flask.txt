$ mkdir app
$ mkdir app/static
$ mkdir app/templates
$ mkdir tmp

app/__init__.py:
from flask import Flask

app = Flask(__name__)
from app import views



app/views.py:
from app import app

@app.route('/')
@app.route('/index')
def index():
    return "Hello, World!"



root run.py
#!/usr/bin/env python
from app import app
app.run(debug=True)


To indicate that the run script is an executable:
    chmod a+x run.py


To run it:
    ./run.py
